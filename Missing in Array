class Solution {
    int missingNum(int arr[]) {
        // code here
        int n = arr.length + 1;
        long sum1 = (long) n * (n+1)/2;
        long sum2 = 0;
        for(int i=0;i<n-1;i++) sum2 += arr[i];
        return (int) (sum1 - sum2);

    }
}
